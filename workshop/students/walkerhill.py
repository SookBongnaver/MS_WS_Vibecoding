def calculate(records, *, slot_ids, date, people, budget, start, end):
    """records maps category to [{record_id, category, payload}].

    Implement itinerary total and date/window/overlap/transfer/capacity/budget
    validation. Example: WS1+WS2, 2 people -> 160000 KRW. Missing price or
    transfer time must require confirmation, not invent a value.
    Run: python -m unittest tests.test_student_contract -v
    """
    raise NotImplementedError("Implement calculate in workshop/students/walkerhill.py with Codex, then run tests; solution mode is an explicit instructor reference.")
